import React from 'react';
import clsx from 'clsx';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: 'Engage',
    imageUrl: 'img/ENGAGE.svg',
    description: (
      <>
          A powerful lead generation tool which turns website visitors into highly qualified leads by demonstrating the affordability of your vehicles.
      </>
    ),
  },
  {
    title: 'Convert',
    imageUrl: 'img/CONVERT.svg',
    description: (
      <>
          An award-winning innovative deal management system which supports e-commerce and bridges the gap between your online and showroom sales processes.
      </>
    ),
  },
  {
    title: 'Transact',
    imageUrl: 'img/TRANSACT.svg',
    description: (
      <>
          Built around specific lender and OEM requirements, our enterprise-level vehicle and financial services retailing platform delivers an omni-channel retail solution for vehicles and financial services. Connecting the consumer, the retailer and the lender together at all stages of the vehicle buying journey.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={clsx('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`Hello from ${siteConfig.title}`}
      description="Documentation for all iVendi Products">
      <header className={clsx('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={clsx(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length > 0 && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map((props, idx) => (
                  <Feature key={idx} {...props} />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}

export default Home;
